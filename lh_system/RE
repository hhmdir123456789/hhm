通达信量化监控系统 - 自动驾驶增强版
软件概述
本软件是一款面向量化交易爱好者和专业投资者的全功能监控与自动交易系统。系统基于PyQt5构建，集成了实时行情监控、技术指标分析、K线图绘制、预警通知、自选股管理、AI策略生成、回测优化、自动驾驶（强化学习执行、GAN压力测试、组合优化）等核心模块。用户可通过图形界面轻松管理股票池，利用AI辅助挖掘因子、生成策略，并启动无人值守的自动驾驶引擎。

主要特点：

实时监控：对接通达信行情接口，实时刷新股票价格、涨跌幅、成交量涨速。

技术分析：内置MA、MACD、RSI、KDJ、布林带宽度等常用指标。

K线图增强：支持日K线、成交量、主力资金流向副图、最高最低标注、垂直参考线、全屏显示、鼠标键盘交互。

自选股与板块：支持创建自定义板块，CSV导入/导出，可与通达信互导。

预警系统：基于价格、涨跌幅、成交量等条件设置预警，系统托盘通知。

AI因子挖掘：基于遗传编程自动生成技术因子，评估因子IC。

AI策略生成：通过Ollama本地模型或DeepSeek API生成策略代码。

回测与优化：真实回测引擎（含滑点、佣金），支持贝叶斯超参数优化。

自动驾驶功能：

强化学习执行器（RLTrader）：使用PPO算法优化订单拆分，降低市场冲击。

GAN压力测试器：生成极端市场情景，评估组合风险。

动态组合配置器：支持均值-方差、风险平价、Black-Litterman优化。

自动驾驶引擎：定时执行调仓，无人值守运行。

配置持久化：监控列表、指标开关、预警条件、自选股、板块等均保存至本地。

功能特性
1. 主界面
股票列表：显示代码、名称、行业、现价、涨跌幅、涨速、技术指标、信号、状态、更新时间。

K线预览：点击表格行，右侧自动显示最近60天的K线预览。

工具栏：刷新数据、配置、导出、清空日志。

菜单栏：

配置：保存配置、编辑监控股票、指标设置。

预警：添加预警、清空预警。

板块管理：管理板块、从通达信CSV导入自选股。

自选股：显示自选股管理窗口。

AI工具：自动因子挖掘、AI策略生成、回测与优化、智能风控设置。

自动驾驶：设置自动驾驶、强化学习执行器、GAN压力测试器、动态组合配置器、自动驾驶引擎。

2. 实时数据监控
对接通达信行情服务器（默认123.125.108.59:7709），支持多只股票同时监控。

每秒更新一次界面，显示最新价、涨跌幅、成交量涨速。

自动识别交易时段，非交易时段显示灰色提示。

3. 技术指标计算
MA：短周期（默认5）和长周期（默认20）均线。

MACD：DIF、DEA、MACD柱（显示DIFF-DEA）。

RSI：相对强弱指数（14周期）。

KDJ：随机指标，显示K值。

布林带宽度：(上轨-下轨)/中轨。

用户可在“指标设置”中开启/关闭各指标的显示。

4. K线图窗口
双击股票行或右键菜单打开。

显示日K线、成交量、主力净流入副图。

自动标注最高价和最低价。

支持鼠标悬停显示详细数据、垂直参考线。

支持滚轮缩放、鼠标拖拽平移、全屏切换（F11）。

上下箭头键可调整显示天数（10~500天）。

5. 预警系统
可针对任意股票设置条件表达式，如price > 1500、change > 5、volume > 100000。

触发时状态栏变色，系统托盘弹出通知（需启用通知）。

条件评估使用安全沙箱，仅允许price、change、volume及abs、max、min、round函数。

6. 自选股与板块管理
自选股管理：通过搜索添加/删除股票，支持CSV导入/导出。

板块管理：可新建/删除板块，为每个板块添加股票，同样支持CSV导入/导出。

通达信互通：可将通达信导出的CSV文件导入为自选股。

7. AI工具
自动因子挖掘
基于gplearn的遗传编程，从历史数据中挖掘新因子。

支持自定义种群大小、代数。

输出新因子及其IC值（与未来5日收益的相关系数）。

AI策略生成
免费本地模型（需安装ollama并运行服务）：选择模型（qwen2.5-coder:7b、llama3.2:3b），输入策略描述，生成Python策略代码。

DeepSeek API（需付费）：输入API Key，根据描述生成代码。

模拟生成：内置示例代码，用于测试。

生成的代码遵循自定义的BaseStrategy接口，包含__init__和next方法。

回测与优化
选择策略文件、股票、初始资金、佣金率、滑点。

运行回测，输出总收益率、夏普比率、最大回撤、最终资金、交易次数。

可选启用贝叶斯优化（需安装scikit-optimize），自动搜索最优参数（如MA周期）。

智能风控设置
动态止损：设置ATR倍数（如2倍ATR）。

仓位管理：单笔风险比例（如2%）。

GNN产业风险监测（预留接口，未实现具体模型）。

8. 自动驾驶功能（高级）
强化学习执行器
使用PPO算法训练交易智能体，学习在给定市场波动下如何拆分大额订单以降低冲击成本。

可训练新模型或加载已有模型，进行模拟执行。

GAN压力测试器
使用生成对抗网络（GAN）生成极端市场情景（收益率序列）。

对给定组合进行压力测试，评估极端情况下可能的最大回撤。

动态组合配置器
支持均值-方差、风险平价、Black-Litterman三种优化方法。

用户选择股票池、目标年化收益、无风险利率、单资产权重上限，计算最优权重并给出组合预期收益、波动率、夏普比率。

自动驾驶引擎
设置目标年化收益、最大回撤容忍度、调仓间隔、是否使用RL执行器和GAN压力测试。

后台线程定时执行：

获取最新数据，计算收益率和协方差矩阵。

使用组合优化器生成目标权重。

通过RL执行器（若启用）生成拆单指令，输出调仓订单。

提供启动/停止控制，实时显示运行日志。

安装与依赖
环境要求
Python 3.7+

Windows / Linux / macOS（建议Windows，因通达信接口依赖）

依赖库
基础依赖（必需）：

text
PyQt5
pandas
numpy
matplotlib
mplfinance
mootdx
akshare
AI/高级功能依赖（可选，缺失对应功能不可用）：

自动因子挖掘：gplearn, scikit-learn

贝叶斯优化：scikit-optimize

AI策略生成（本地）：ollama

AI策略生成（DeepSeek）：openai

强化学习：stable-baselines3, gym

GAN压力测试：torch

组合优化：cvxpy

安装步骤
克隆代码或下载源文件。

安装基础依赖：

bash
pip install PyQt5 pandas numpy matplotlib mplfinance mootdx akshare
根据需要安装可选依赖：

bash
pip install gplearn scikit-learn scikit-optimize ollama openai stable-baselines3 gym torch cvxpy
若使用Ollama本地模型，需先安装Ollama并下载模型：

bash
ollama pull qwen2.5-coder:7b
运行软件：

bash
python quant_monitor.py
使用说明
初次启动
首次启动时，监控列表默认为['600519', '000001']。

若通达信行情服务器不可达，会尝试从本地读取历史数据（需配置正确的TDX目录，默认C:/zd_zsone）。

可在“配置”中修改服务器地址、端口、刷新间隔等。

添加/删除监控股票
菜单栏“配置” → “编辑监控股票”，弹出股票选择窗口，支持搜索、分页，勾选后确定即可。

软件会自动刷新行情数据。

查看K线图
双击表格中的某行，或右键选择“打开详细K线图”。

在K线图窗口中，可用鼠标滚轮缩放，拖拽平移，按F11全屏。

设置预警
右键某股票，选择“设置预警”，输入条件表达式（如price>100）。

或通过菜单“预警” → “添加预警”进行全局设置。

管理自选股和板块
自选股：菜单“自选股” → “显示自选股”，可添加/删除股票。

板块：菜单“板块管理” → “管理板块”，可新建板块、添加股票、导入/导出CSV。

使用AI工具
因子挖掘：菜单“AI工具” → “自动因子挖掘”，选择股票和代数，点击“开始挖掘”，结果将显示新因子及IC。

策略生成：选择AI方案（本地/DeepSeek/模拟），输入策略描述，生成代码后保存为.py文件。

回测与优化：选择策略文件、股票、参数，运行回测；勾选“启用贝叶斯优化”可进行超参数搜索。

风控设置：调整止损倍数和单笔风险比例。

启动自动驾驶
菜单“自动驾驶” → “设置自动驾驶”，打开设置窗口。

填入目标年化收益、最大回撤容忍度、调仓间隔。

根据需要启用强化学习执行器（需提前训练模型）和GAN压力测试。

点击“启动自动驾驶”，引擎将在后台运行，定时调仓。

注意：自动驾驶依赖cvxpy、stable-baselines3、torch等库，未安装时对应选项不可用。

模块说明
数据获取模块
DataFetcher：后台线程，通过mootdx连接通达信服务器，定时拉取行情数据。

Reader：本地TDX数据读取，用于历史K线和技术指标计算。

技术分析模块
在QuantMonitor类中实现calc_macd、calc_rsi、calc_kdj_k、calc_boll_width方法。

AI模块
AutoFactorMiner：因子挖掘类，封装gplearn。

StrategyGenerator及其子类：策略生成器，支持Ollama、DeepSeek、模拟。

BacktestEngine：回测引擎，含滑点、佣金、贝叶斯优化。

RiskManager：风控管理（止损、仓位）。

自动驾驶模块
RLTrader：强化学习执行器，封装stable-baselines3。

GANStressTester：GAN压力测试器，基于PyTorch。

PortfolioOptimizer：组合优化器，基于cvxpy。

AutoPortfolioManager：组合管理器，结合用户目标与风险约束。

AutoPilotEngine：后台调仓引擎，继承QThread。

UI模块
QuantMonitor：主窗口，集成表格、菜单、工具栏、状态栏。

KLineDialog：K线图窗口，支持交互。

FavoritesManagerDialog：自选股管理对话框。

GroupManagerDialog：板块管理对话框。

各类辅助对话框（AI因子、策略生成、回测、风控、自动驾驶设置等）。

注意事项
通达信行情服务器：默认IP为123.125.108.59，端口7709。若无法连接，请更换为可用的行情服务器（如121.14.107.54:7709等）。

TDX数据目录：需正确设置通达信安装目录，否则本地历史数据读取失败。

AKShare网络依赖：股票名称和行业数据依赖AKShare，需要网络连接。

非交易时段：软件仍可刷新，但数据不会变化；预警在非交易时段无效。

AI功能依赖：部分AI功能需要额外安装库，缺失时菜单项会禁用或提示。

回测数据准确性：回测引擎使用收盘价模拟买卖，未考虑停牌、涨停限制等细节，结果仅供参考。

自动驾驶风险：自动驾驶引擎会实际生成调仓订单（仅输出到日志，未对接实盘），用于模拟或后续对接实盘交易接口。

性能：监控股票数量过多可能导致界面卡顿，建议控制在50只以内。

扩展与二次开发
本软件采用模块化设计，开发者可轻松扩展：

新增技术指标：在QuantMonitor类中添加计算方法，并在表格更新逻辑中集成。

新增AI策略生成器：继承StrategyGenerator类，实现generate方法。

对接实盘交易：修改AutoPilotEngine中的订单生成部分，调用交易API。

自定义预警条件：扩展safe_eval允许的变量和函数。

常见问题
Q: 启动后无法获取行情数据怎么办？
A: 检查网络连接，确保行情服务器IP和端口正确，可在配置中修改。若使用本地TDX，请检查TDX目录路径。

Q: 回测时提示“scikit-optimize未安装”？
A: 如需贝叶斯优化，请执行pip install scikit-optimize。

Q: 强化学习执行器训练失败？
A: 确保已安装stable-baselines3和gym，且历史数据量足够（至少100个交易日）。

Q: 如何将自选股导出给通达信？
A: 在板块管理或自选股管理中使用“导出CSV”，通达信可通过“系统” → “数据维护” → “自选股导入”导入。

版本历史
v5.0（当前版本）：集成自动驾驶增强功能（RL、GAN、组合优化、自动驾驶引擎），完善AI工具链，优化界面交互。

v4.0：增加AI因子挖掘、AI策略生成、回测优化。

v3.0：增加K线图增强、自选股板块管理。

v2.0：增加技术指标、预警系统。

v1.0：基础实时监控。

联系方式
如有问题或建议，请联系开发者（假设）。软件遵循MIT开源协议，欢迎贡献代码。